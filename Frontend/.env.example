# Ellcworth Frontend (.env.example)
# Copy to: Frontend/.env
# Vite exposes only variables prefixed with VITE_

# API base URL used by customer portal pages/services
# Your backend listens on PORT (default 8000) and mounts canonical v1 routes under /api/v1.
VITE_API_BASE_URL=http://localhost:8000
