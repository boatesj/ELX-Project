# Ellcworth BackgroundServices (.env.example)
# Copy to: BackgroundServices/.env
# Keep this minimal for now (Phase 5 readiness).
# Add provider-specific keys only when the service actually uses them.

NODE_ENV=development
PORT=8100

# If background services call the backend API:
API_BASE_URL=http://localhost:8000

# If background services verify/issue JWTs (only if used):
JWT_SECRET=change_me_to_a_long_random_string

# Optional logging
LOG_LEVEL=info
